<div class="super-form">
  <%= simple_form_for @note, url: notes_path(query: params[:query]) do |f| %>
    <%= f.input :description %>
    <%= f.submit "Save", class: "super-button" %>
  <% end %>
</div>
